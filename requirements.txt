Flask>=3.0
# America/Toronto timezone data for the retry rule, for systems without a
# system tz database (Linux containers, some Windows setups). Harmless on macOS.
tzdata
# Test suite: run with  python -m pytest tests/
pytest
