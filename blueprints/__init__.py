# Marks blueprints/ as a Python package so its modules import as blueprints.auth,
# blueprints.queue, etc. The package name also keeps blueprints/queue.py from
# shadowing Python's built-in `queue` module.
